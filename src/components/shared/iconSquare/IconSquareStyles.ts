import { StyleSheet } from "react-native";
import Colors from "../../../constants/Colors";

export const iconSquareStyles = StyleSheet.create({
  container: {
    backgroundColor: Colors.light.gray1,
    borderColor: Colors.light.brandLight,
    borderWidth: 1,
  },
});
